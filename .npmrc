# Refuse to install if the local Node/pnpm versions fall outside "engines".
engine-strict=true

# Hoist nothing implicitly: dependencies must be declared to be importable.
# This is the stricter dependency management the migration is after.
shamefully-hoist=false

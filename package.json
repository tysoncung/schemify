{
  "name": "schemify",
  "version": "0.1.0",
  "description": "JSON → TypeScript, Zod, GraphQL, OpenAPI, SQL, C#, and more. Developer data transformation toolkit.",
  "type": "module",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "bin": {
    "schemify": "dist/cli.js"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "pnpm run lint && pnpm run build && pnpm run test:run"
  },
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.3",
    "vitest": "^4.1.10",
    "zod": "^4.4.3"
  }
}
